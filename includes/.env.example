# MMM – environment variable template
# Copy this file to includes/.env and fill in your values.
# Lines starting with # are ignored.  Quotes around values are optional.
#
# IMPORTANT: Never commit includes/.env to version control!
# The .gitignore already excludes it.

# ---------------------------------------------------------------------------
# Database – live / production environment (urtyp_live_internet)
# ---------------------------------------------------------------------------
LIVE_DB_HOST=localhost
LIVE_DB_NAME=mmmdb
LIVE_DB_USER=mmmuser
LIVE_DB_PASS=changeme

# ---------------------------------------------------------------------------
# Database – internet-facing dev environment (urtyp_dev_internet)
# ---------------------------------------------------------------------------
DEV_DB_HOST=localhost
DEV_DB_NAME=mmmdb
DEV_DB_USER=mmmuser
DEV_DB_PASS=changeme

# ---------------------------------------------------------------------------
# Database – intranet LAN environment (urtyp_dev_intranet / madnix_live)
# ---------------------------------------------------------------------------
LAN_DB_HOST=localhost
LAN_DB_NAME=mmmdb
LAN_DB_USER=mmmuser
LAN_DB_PASS=changeme

# ---------------------------------------------------------------------------
# Mail – transactional (SMTP)
# ---------------------------------------------------------------------------
MAIL_HOST=smtp.example.com
MAIL_USERNAME=mail@example.com
MAIL_PASSWORD=

# ---------------------------------------------------------------------------
# Mail – newsletter (separate SMTP account)
# ---------------------------------------------------------------------------
MAIL_HOST_NEWSLETTER=smtp.example.com
MAIL_USERNAME_NEWSLETTER=newsletter@example.com
MAIL_PASSWORD_NEWSLETTER=

# ---------------------------------------------------------------------------
# External APIs
# ---------------------------------------------------------------------------
# Bing Maps API key (used by the geek-radar / attendee location map)
BINGMAPS_KEY=

# ---------------------------------------------------------------------------
# Filesystem overrides (optional – defaults are hard-coded in constants.php)
# ---------------------------------------------------------------------------
# LIVE_PELASDIR=/var/www/pelas/
# LIVE_SMARTY_BASE_DIR=/var/www/includes/smarty/
